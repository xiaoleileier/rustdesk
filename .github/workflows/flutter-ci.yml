name: Full Flutter CI

# Disabled auto-trigger for the gfyc fork (manual-only) to avoid noisy failures.
on:
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
