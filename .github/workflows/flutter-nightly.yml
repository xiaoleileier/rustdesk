name: Flutter Nightly Build

# Disabled nightly schedule for the gfyc fork (manual-only).
on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
